{
  "name": "aura-monorepo",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "build": "npm run build:frontend && npm run build:backend && cp -r frontend/dist/frontend/browser backend/public",
    "start": "cd backend && npm run start:prod"
  }
}
